# When built and served by FastAPI (production/start.py) use a relative path.
# When running `npm run dev` the Vite proxy forwards /api to 127.0.0.1:8007.
VITE_API_BASE_URL=/api/v1
# Base (copy/mimic) server — used as the default guild for the dashboard.
VITE_DEFAULT_GUILD_ID=751274186189701190
